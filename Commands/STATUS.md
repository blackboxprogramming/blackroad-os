# Commands — STATUS

**State:** scaffold (README only)
**Implementation:** planned — the 17 core commands are named in canon but not yet enumerated here or in a registry
**Receipts:** none yet

## What exists
- `README.md` — purpose and boundary (operator command surface; operator-readable first)
- Live command routing exists only in `index.html` (the desktop's `ROUTES` / palette)

## Next
- Enumerate the 17 core commands as a `Registry/commands.json` (with a schema + validator), the same single-source pattern used for products/agents.
