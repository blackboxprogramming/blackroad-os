{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BlackRoad RoadMap Lane",
  "description": "Canonical record for one of the 20 RoadMap build-status lanes. Registry/lanes.json is the single source of truth; the LANES array in index.html is generated from it via scripts/sync-collections.mjs.",
  "type": "object",
  "additionalProperties": false,
  "required": ["num", "id", "name", "status", "meaning"],
  "properties": {
    "num": { "type": "string", "pattern": "^[0-9]{2}$", "description": "Two-digit lane position, 01-20." },
    "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
    "name": { "type": "string" },
    "status": { "enum": ["done", "fake_done", "next", "planned"], "description": "Honest status. 'fake_done' = claimed done but not yet backed by a receipt (per CANON)." },
    "meaning": { "type": "string" },
    "currentlyExists": { "type": "array", "items": { "type": "string" } },
    "needs": { "type": "array", "items": { "type": "string" } },
    "blockers": { "type": "array", "items": { "type": "string" } },
    "proofNeeded": { "type": "array", "items": { "type": "object" } },
    "next": { "type": "string" },
    "fakeNote": { "type": "string", "description": "Why a lane is fake_done, or what proof is still missing." }
  }
}
