# HighWay/ (Product 18)

Infrastructure, compute, routing, cloud, fleet, nodes, domains, TLS, model serving, observability, local hardware, Pi clusters, and deployment roads.

**Unique layer:** Hybrid mesh backbone (Meshtastic LoRa radio mesh + Headscale/WireGuard IP overlay) with intelligent network hopping, Git-native sync, and RoadChain provenance.

This is the physical + virtual "road" that makes the entire BlackRoad OS work across disconnected and local environments.